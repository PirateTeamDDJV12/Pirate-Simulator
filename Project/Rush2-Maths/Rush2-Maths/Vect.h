//template <typename valueType>
/*class Vect
{
private:
    float m_X;
    float m_Y;
    float m_Z;
public:
    float x() const noexcept
    {
        return m_X;
    }

    float y() const noexcept
    {
        return m_Y;
    }

    float z() const noexcept
    {
        return m_Z;
    }

    void x(float newValue) noexcept
    {
        m_X = newValue;
    }

    void y(float newValue) noexcept
    {
        m_Y = newValue;
    }

    void z(float newValue) noexcept
    {
        m_Z = newValue;
    }



  

    float length2() const noexcept
    {
        return ((x() * x()) + (y() * y()) + (z() * z()));
    }

    float length() const noexcept;

    bool isOrigin() const noexcept
    {
        return ((x() == 0.f) && y() == 0.f && z() == 0.f);
    }

public:
    /*
    vect1 . vect2
   
   //s static float scalarProduct(const Vect& vect1, const Vect& vect2) noexcept = 0;


    virtual  float length2(const Vect& vect) noexcept = 0;
    virtual  float length(const Vect& vect) noexcept = 0;


public:
    virtual bool operator==(const Vect& other) const noexcept = 0;

    virtual bool operator!=(const Vect& other) const noexcept = 0;

    virtual Vect& operator+=(const Vect& other) noexcept = 0;
    virtual Vect& operator-=(const Vect& other) noexcept = 0;
    virtual Vect& operator*=(const Vect& other) noexcept = 0;
    virtual Vect& operator*=(float scalar) noexcept = 0;
    virtual Vect& operator/=(float scalar) noexcept = 0;


    virtual Vect& operator+(Vect& other) const noexcept = 0;

    virtual Vect& operator-(const Vect& other) const noexcept = 0;


    virtual Vect& operator*(float scalar) const noexcept = 0;


    virtual Vect& operator/(float scalar) const noexcept = 0;



};*/